/**
 * POST /api/mint/execute-v3
 *
 * Explicit V3 route for the Issuer Portal version picker. The implementation
 * remains in execute.ts to preserve the production route already deployed.
 */
export { default } from "./execute.js";
